import 'package:flutter/material.dart';

class AppColors {
  // ChatGPT-style Dark Theme (but a shade darker)
  static const darkBackground = Color(0xFF0D0D0D); // Very dark, almost black
  static const darkSurface = Color(
    0xFF171717,
  ); // Slightly lighter for cards/containers
  static const darkSurfaceSecondary = Color(0xFF212121);
  static const darkBorder = Color(0xFF262626); // Subtle border color
  static const darkDivider = Color(0xFF262626);

  static const textPrimary = Color(0xFFECECEC); // Off-white for readability
  static const textSecondary = Color(
    0xFFB4B4B4,
  ); // Grey for less important text

  static const accentBlue = Color(0xFF2D7FF9); // Modern accent blue
  static const errorRed = Color(0xFFEF4444);
}
