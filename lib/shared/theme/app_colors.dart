import 'package:flutter/material.dart';

class AppColors {
  // deep black canvas — striking, not washed-out grey
  static const darkBackground = Color(0xFF050505);
  static const darkSurface = Color(0xFF121212);
  static const darkSurfaceSecondary = Color(0xFF1A1A1A);
  static const darkBorder = Color(0xFF2A2A2A);
  static const darkDivider = Color(0xFF2A2A2A);

  static const textPrimary = Color(0xFFF5F5F5);
  static const textSecondary = Color(0xFFC8C8C8);

  static const accent = Color(0xFFA1A1AA); // brighter zinc for icons/active
  static const errorRed = Color(0xFFEF4444);
}
